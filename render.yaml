# Render Blueprint for Audio to ISL
# https://render.com/docs/blueprint-spec

services:
  # Backend API Service
  - type: web
    name: audio-to-isl-api
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: ALLOWED_ORIGINS
        value: https://your-frontend-url.onrender.com
    healthCheckPath: /health

  # Frontend Static Site (optional - if hosting frontend on Render too)
  # - type: web
  #   name: audio-to-isl-frontend
  #   runtime: static
  #   rootDir: frontend
  #   buildCommand: npm install && npm run build
  #   staticPublishPath: dist
  #   headers:
  #     - path: /*
  #       name: Cache-Control
  #       value: no-cache
  #   envVars:
  #     - key: VITE_API_URL
  #       value: https://audio-to-isl-api.onrender.com

